//struct sockaddr_in {
//    short            sin_family;   // Address family (e.g., AF_INET for IPv4)
//    unsigned short   sin_port;     // Port number (in network byte order)
//    struct in_addr   sin_addr;     // IP address (in network byte order)
//    char             sin_zero[8]; // Padding to match `struct sockaddr` size
//};

//struct sockaddr {
//    unsigned short sa_family;   // Address family (e.g., AF_INET for IPv4)
//    char sa_data[14];           // 14 bytes of protocol-specific address data
//};


Server-side code: Uses accept() to handle client connections.
Client-side code: Uses connect() to initiate a connection to the server.